# RoutinePlus: IsWorkday - 产品架构设计白皮书

## 一、 产品定位与核心理念

**RoutinePlus: IsWorkday** 是专为三星“模式与日常程序”设计的系统级逻辑插件。

* **核心功能**：基于中国法定节假日与调休规则，提供精准的闹钟自动化调度。
* **核心架构 (Local-First)**：以本地 Room 数据库为核心驱动，弱化网络依赖，确保离线环境下的毫秒级响应。
* **解耦原则**：将“物理系统时间”与“业务目标校验日”彻底剥离。

---

## 二、 数据架构与模型映射 (Data Layer)

### 1. 网络数据源与容灾机制

采用高可用双链路架构，避免单一 CDN 节点污染或阻断。

* **主链路 (Primary)**：`[https://cdn.jsdelivr.net/gh/NateScarlet/holiday-cn@master/](https://cdn.jsdelivr.net/gh/NateScarlet/holiday-cn@master/){年份}.json`
* **备链路 (Fallback)**：`[https://fastly.jsdelivr.net/gh/NateScarlet/holiday-cn@master/](https://fastly.jsdelivr.net/gh/NateScarlet/holiday-cn@master/){年份}.json`
* *注：网络拦截器配置 3 秒超时，主链路超时自动降级至备链路。不建议在生产环境使用 raw.githubusercontent 直连。*

### 2. 数据模型转换 (JSON to Entity)

根据最新的 API 规范，构建 Kotlin 序列化数据类与 Room 数据库实体：

```kotlin
// 1. 网络响应数据类
@Serializable
data class HolidayYearResponse(
    val year: Int,
    val papers: List<String>,
    val days: List<HolidayDay>
)

@Serializable
data class HolidayDay(
    val name: String,
    val date: String,       // ISO 8601 格式，如 "2026-10-01"
    val isOffDay: Boolean   // true 为休息日，false 为补班
)

// 2. Room 数据库实体 (仅存储核心的 days 数组)
@Entity(tableName = "holiday_days")
data class HolidayEntity(
    @PrimaryKey val date: String,
    val name: String,
    val isOffDay: Boolean
)

```

---

## 三、 核心调度与判定算法 (Core Logic)

### 1. 跨时区计算的强制性约束 (时差漂移防御)

在跨国界或跨时区场景下（例如设备当前物理位置处于北美太平洋时区 PDT，系统时间为 5 月 17 日清晨 6 点，而对应的北京时间已是 5 月 17 日晚 21 点），若依赖默认系统时区，极易在跨越午夜时产生长达一整天的判定错位。

**强制约束**：应用全局所有涉及日历计算的逻辑，必须强行锚定东八区：

```kotlin
val chinaZoneId = ZoneId.of("Asia/Shanghai")
// targetOffset: 0 (今天) 或 1 (明天)
val targetDate = LocalDate.now(chinaZoneId).plusDays(targetOffset)

```

### 2. 跨年防漏算法 (联合合并查询)

无论当前月份，数据同步与判定始终并发请求并合并 `[目标年份]` 与 `[目标年份 + 1]` 的数据，彻底消灭 12 月末调休记录归属下一年文件而导致的判定盲区。

```kotlin
val targetYear = targetDate.year
// 从本地数据库查询 (数据库由后台 WorkManager 维护跨年合并后的完整集合)
val dayRecord = roomDatabase.holidayDao().getDayByDate(targetDate.toString())

```

---

## 四、 自动化触发协议 (Shortcut Intent Payload)

三星“日常程序”通过唤起本应用注册的动态快捷方式来执行任务。UI 界面的配置最终将序列化为以下 Intent 参数结构，由无界面的 `BroadcastReceiver` 或 `Worker` 接管执行：

```json
{
  "action": "com.routineplus.isworkday.ACTION_EXECUTE",
  "payload": {
    "target_offset": 1,          // 目标校验日：0=今天，1=明天
    "condition_mode": "WORKDAY", // 执行条件：WORKDAY / OFFDAY / ALWAYS
    "alarm_hour": 8,             // 设定小时 (0-23)
    "alarm_minute": 30,          // 设定分钟 (0-59)
    "alarm_label": "通勤闹钟"      // 闹钟标签，用于覆盖与去重
  }
}

```

---

## 五、 UI/UX 设计规范 (Material 3 + One UI)

界面采用 Material You 动态色彩，匹配三星系统的视觉语言。整体布局分为三大区块：

### 区块 A：状态面板与可视化日历 (Dashboard)

* **状态头**：显示“数据同步至 202X 年”，并提供手动强制同步图标。
* **月历组件 (Month View)**：
* 通过 `LazyVerticalGrid` 渲染当月视图。
* 数据源直接观察 Room 数据库的 `Flow`。
* **视觉标识**：法定节假日（红色日期 + “休”字角标），调休补班（深色日期 + “班”字角标）。



### 区块 B：执行规则配置 (Rule Configurator)

* **校验目标 (Target)**：使用 `SegmentedButton` 选择【今天 Today】或【明天 Tomorrow】。明确提示“今天”适用于跨零点后的凌晨触发场景。
* **判定条件 (Condition)**：下拉选择（仅工作日 / 仅休息日 / 每天）。
* **闹钟设定 (Alarm Settings)**：提供 `TimePicker` 和标签输入框。

### 区块 C：底栏操作区 (Action Area)

* 底部居中放置 `ExtendedFloatingActionButton`，文案为“生成日常快捷指令”。点击后调用 `ShortcutManager` 向系统写入入口。

---

## 六、 时光机沙盒测试 (Dry Run Sandbox)

为保证配置的绝对可靠，界面底部集成“沙盒终端”区域，允许注入虚拟时间与参数进行预演，避免产生真实的系统废弃闹钟。

**测试终端输出示例：**

```text
> [系统] 启动 R+ 模拟沙盒...
> [注入] 锁定标准时区: Asia/Shanghai (UTC+8)
> [模拟] 设定触发时间为: 2026-12-31 23:30
> [读取] 读取参数 -> 目标偏移量: +1 天, 条件: WORKDAY
> [计算] 目标校验日: 2027-01-01 (星期五)
> [本地] 查询数据库命中 -> "元旦", isOffDay: true
> [判定] 不满足 WORKDAY 触发条件
> [结果] ✅ 拦截成功：已跳过闹钟创建。

```